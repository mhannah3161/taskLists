{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run build && npm run preview",
    "start": "vite",
    "build": "vite build",
    "lint": "eslint src --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "render-build": "npm install and npm run build"
  },
  "dependencies": {
    "mongoose": "^8.0.4",
    "react": "^18.2.0"
  }
}
